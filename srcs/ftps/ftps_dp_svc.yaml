apiVersion: v1
kind: Service
metadata:
  name: ftps-service
  annotations:
    metallb.universe.tf/allow-shared-ip: shared
  labels:
    run: ftps
spec:
  type: LoadBalancer
  selector:
    app: ftps
  ports:
    - port: 21

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: ftps-deploy
  labels:
    app: ftps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ftps
  template:
    metadata:
      labels:
        app: ftps
    spec:
      resources :
      containers:
      - name: ftps
        imagePullPolicy: Never
        image: ftp
        ports:
          - containerPort: 21
    